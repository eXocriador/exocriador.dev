# exocriador.dev - Потребує виправлення

## ❌ Проблема

Проєкт НЕ збирається через помилки TypeScript у старому коді:

```
src/middleware/errorHandler.ts - невикористані параметри
src/routes/contact.ts - неправильні типи Promise
```

## ✅ Що працює

- ✅ **tavern.exocriador.dev** - працює з auto-SSL
- ✅ **tyusha.love** - працює з auto-SSL

## 🔧 Як виправити exocriador.dev

### Варіант 1: Виправити код (рекомендовано)

```bash
cd /home/exocriador/apps/exocriador.dev

# Виправте помилки в файлах:
# - backend/src/middleware/errorHandler.ts (додайте _ до невикористаних параметрів)
# - backend/src/routes/contact.ts (виправте типи Promise)

# Потім зберіть:
docker compose build
docker compose up -d
```

### Варіант 2: Використати старий код без TypeScript

Замініть `npm run build` на просто копіювання `.ts` файлів у Dockerfile.

### Варіант 3: Створити простий placeholder

Створіть простий static сайт як placeholder поки не виправите код.

## 📝 Примітки

- Frontend і telegram-bot збудувалися успішно
- Проблема тільки в backend
- Всі інші додатки (tavern, tyusha) працюють нормально з Traefik

## 🎯 Після виправлення

Після того як виправите код, просто:

```bash
cd /home/exocriador/apps/exocriador.dev
docker compose up -d --build
```

Traefik автоматично:
- Виявить новий сервіс
- Отримає SSL сертифікат
- Налаштує роутинг для exocriador.dev
